# TODO - Refinement POS Product Card Grid

## Step 1
- [ ] Update `resources/views/transaksi/form.blade.php` product card grid markup/classes only.
- [ ] Add selected/active state styling for cards.

## Step 2
- [ ] Add Alpine `selectedProductId` state in `posDashboard`.
- [ ] Set `selectedProductId` inside `addToCart(product)`.

## Step 3
- [ ] Implement hover-show "Tambah ke Keranjang" button (desktop hover) and selected-show on mobile.
- [ ] Add smooth zoom + hover lift animations.
- [ ] Add soft shadow, category badge on card, stock badge premium.

## Step 4
- [ ] Ensure responsive grid spacing and typography improvements (clamp description, cleaner text styles).

## Step 5
- [ ] Sanity check: do not change cart/sidebar structure and do not break hidden inputs submission.

